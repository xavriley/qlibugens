####################################################################################################
# CMakeLists file for Qlib UGens
# Generated by Xavier Riley
# 2021-01-16
####################################################################################################

####################################################################################################
# basic project config
cmake_minimum_required(VERSION 3.5)
set(project_name "QlibUGens")
set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake_modules ${CMAKE_MODULE_PATH})
set(CMAKE_CXX_STANDARD 17)
# set(CMAKE_INSTALL_PREFIX "/Users/xavriley/Library/Application Support/SuperCollider/Extensions" CACHE PATH "install here" FORCE)

####################################################################################################
# load modules
include(SuperColliderServerPlugin RESULT_VARIABLE server_plugin_found)
if(NOT server_plugin_found)
    message(FATAL_ERROR "Could not find server plugin functions module")
endif()

include(SuperColliderCompilerConfig RESULT_VARIABLE compiler_config_found)
if(NOT compiler_config_found)
    message(FATAL_ERROR "Could not find compiler config module")
endif()

# Windows - puts redistributable DLLs in install directory
include(InstallRequiredSystemLibraries)

sc_check_sc_path("${SC_PATH}")
message(STATUS "Found SuperCollider: ${SC_PATH}")
set(SC_PATH "${SC_PATH}" CACHE PATH
    "Path to SuperCollider source. Relative paths are treated as relative to this script" FORCE)

include("${SC_PATH}/SCVersion.txt")
message(STATUS "Building plugins for SuperCollider version: ${SC_VERSION}")

# set project here to avoid SCVersion.txt clobbering our version info
project(${project_name})
sc_do_initial_compiler_config() # do after setting project so compiler ID is available

if(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT OR NOT CMAKE_INSTALL_PREFIX)
    message(WARNING "No install prefix provided, defaulting to $BUILD_DIR/install")
    set(CMAKE_INSTALL_PREFIX "${CMAKE_BINARY_DIR}/install" CACHE PATH "Install prefix" FORCE)
endif()

message(STATUS "Install directory set to: ${CMAKE_INSTALL_PREFIX}")

####################################################################################################
# options
option(SUPERNOVA "Build plugins for supernova" ON)
option(SCSYNTH "Build plugins for scsynth" ON)
option(NATIVE "Optimize for native architecture" OFF)
option(STRICT "Use strict warning flags" OFF)
option(NOVA_SIMD "Build plugins with nova-simd support." ON)

####################################################################################################
# include libraries

if (NOVA_SIMD)
	add_definitions(-DNOVA_SIMD)
	include_directories(${SC_PATH}/external_libraries/nova-simd)
endif()

####################################################################################################
# Begin target BitstreamAutocorrelation

set(BitstreamAutocorrelation_cpp_files
    plugins/BitstreamAutocorrelation/BitstreamAutocorrelation.cpp
    plugins/BitstreamAutocorrelation/BitstreamAutocorrelation.hpp
)
set(BitstreamAutocorrelation_sc_files
    plugins/BitstreamAutocorrelation/BitstreamAutocorrelation.sc
)
set(BitstreamAutocorrelation_schelp_files
    plugins/BitstreamAutocorrelation/BitstreamAutocorrelation.schelp
)


sc_add_server_plugin(
    "QlibUGens/BitstreamAutocorrelation" # desination directory
    "BitstreamAutocorrelation" # target name
    "${BitstreamAutocorrelation_cpp_files}"
    "${BitstreamAutocorrelation_sc_files}"
    "${BitstreamAutocorrelation_schelp_files}"
)

# This external project method works but ran into race issues on Github CI
# include(ExternalProject) # Needs to be included first
# set(EXTERNALS_DIR ${CMAKE_BINARY_DIR}/external)
# ExternalProject_Add(q
# 		PREFIX ${EXTERNALS_DIR}
# 		GIT_REPOSITORY https://github.com/cycfi/q.git
# 		GIT_TAG        origin/master
# 		STEP_TARGETS   build
# 		# BUILD_ALWAYS OFF
# 		CMAKE_CACHE_ARGS
# 		-DQ_BUILD_IO:BOOL=OFF
# 		-DQ_BUILD_TEST:BOOL=OFF
# 		-DQ_BUILD_EXAMPLES:BOOL=OFF
# 		)
# include_directories(${EXTERNALS_DIR}/src/q/q_lib/include)
# include_directories(${EXTERNALS_DIR}/src/q/infra/include)
# add_library(libq INTERFACE)
# add_dependencies(libq ${project_name})
# target_include_directories(libq INTERFACE ${EXTERNALS_DIR}/src/q/qlib/include)
# target_include_directories(libq INTERFACE ${EXTERNALS_DIR}/src/q/infra/include)


set(Q_DIR lib/q)

option(Q_BUILD_EXAMPLES "" OFF)
option(Q_BUILD_TEST "" OFF)
option(Q_BUILD_IO "" OFF)

add_subdirectory(${Q_DIR})
include_directories(${Q_DIR}/q_lib/include)
include_directories(${Q_DIR}/infra/include)
add_library(qlib INTERFACE)

# add_dependencies(qlib ${project_name})
#target_link_libraries(${project_name}
#	PUBLIC
#	libq)

#target_include_directories(qlib INTERFACE ${Q_DIR}/q_lib/include)
#target_include_directories(qlib INTERFACE ${Q_DIR}/infra/include)
set_target_properties(qlib PROPERTIES INTERFACE_SYSTEM_INCLUDE_DIRECTORIES $<TARGET_PROPERTY:qlib,INTERFACE_INCLUDE_DIRECTORIES>)

# End target BitstreamAutocorrelation
####################################################################################################

####################################################################################################
# END PLUGIN TARGET DEFINITION
####################################################################################################

message(STATUS "Generating plugin targets done")
